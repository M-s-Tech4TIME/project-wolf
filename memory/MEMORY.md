# Memory index

- [Wazuh active-response contract](wazuh-active-response-contract.md) — REFERENCE (2026-06-19): PUT /active-response accepts ONLY command/arguments/alert (no custom/timeout/location → 400); command must be `!`-prefixed; target via alert.data.srcip (numeric IP) / alert.data.dstuser; no per-call timeout; HTTP-200-on-failure. Verified live v4.14.3 + source v4.14.3/v4.14.5. Source of truth: `wolf_server/wazuh/active_response.py` + `docs/reference/wazuh-active-response.md`. Fixes the 6-b firewall-drop bug
- [CI audit before push](ci-audit-before-push.md) — STANDING RULE (2026-06-11): before EVERY push, audit .github/workflows for slice-relevant changes (paths, strict-mypy set, smoke loops, artifacts) and land them in the same commit; watch the run to green — CI closeout is part of the slice

- [Integrity across the stack](integrity-across-the-stack.md) — STANDING RULE (2026-05-30): every change preserves integrity across frontend / backend / DB / libraries / UI; full backend suite + cross-organization gate on every services/ change
- [Quality + secure coding discipline](quality-secure-coding-discipline.md) — STANDING RULE (2026-05-31): features-first; quality + secure coding applied inline as each slice is built; dedicated hardening + audit pass deferred to a later phase but tracked, never abandoned
- [No unaddressed errors](no-unaddressed-errors.md) — STANDING RULE (2026-06-01, REAFFIRMED 2026-06-11, SHARPENED 2026-06-16): never leave errors / fails / warnings / skips unaddressed; "addressed" = solved/fixed/patched at the root — never filtered, ignored, or bypassed; "pre-existing baseline" is not a pass. 2026-06-16: closes the optional-dep carve-out — ALL kinds of skips (importorskip/skipif/xfail/network/hardware) must be refactored away (stub the boundary), not tolerated
- [Periodic plan-sync](periodic-plan-sync.md) — STANDING RULE (2026-06-04): proactively audit roadmap / architecture / ADRs / PROGRESS for drift vs shipped work between phase transitions; surface findings before user asks
- [Graphify-first discipline](graphify-first-discipline.md) — STANDING RULE (2026-06-05): when graphify-out/graph.json exists, use graphify query/path/explain FIRST for architectural questions, before grep/Read sweeps
- [Wolf bootstrap superuser flow](wolf-bootstrap-superuser-flow.md) — STANDING RULE (2026-06-10): single Superuser "Wolf" (autogenerated password) ships at install; only role allowed to create organizations + users + roles + configure Wazuh component mapping
- [Shell-wrapper-required pattern](shell-wrapper-required-pattern.md) — STANDING RULE (2026-06-10): every supporting tool ships as Python core + shell wrapper; Python core can only be invoked via the wrapper (audit, permission, env preflight)
- [Tenant renamed to organization](tenant-renamed-to-organization.md) — COMPLETED 2026-06-11 (Phase 6.4): codebase rename landed in one PR (~140 files, 1500+ substitutions, 8 files renamed); rule now historical, "organization" is the canonical name everywhere
- [Web-first configurability](web-first-configurability.md) — STANDING RULE (2026-06-10): every configurable Wolf knob must have a GUI surface; CLI ↔ GUI sync via DB as source of truth; audit-log every config change
- [Per-slice web-test checkpoints](per-slice-web-test-checkpoints.md) — reset → Claude self-validates → reset → user manually tests
- [Multi-slice batch handover](multi-slice-batch-handover.md) — 2026-05-30 ACTIVE for 5.0c-i → 5.0c-l: feedback-set-based handover with two planned bundles (i+j UI polish, k+l stream lifecycle); per-slice integrity gate still mandatory
- [Grounding enrichment (COMMITTED Phase 6.13)](grounding-enrichment-tools-future-phase.md) — operator mandate 2026-07-05 (from the 6-f.3 web-test: correct docs-sourced answers still mostly Uncertain/Not-Verified): fully dedicated phase for enhancing grounding across EVERY aspect — more Verified verdicts accurately (verification/justification/validation, never loosening the judge); scope = web-research evidence source-tier-aware for the judge + more evidence-supplying tools + per-claim evidence selection (ex-ADR-0026 `cited`) + judge/verdict quality + calibration harness; own ADR when opened; sequencing operator-driven ("later"); continuous evaluate-every-tool-for-evidence-value rule stays in force meanwhile
- [Verdict rename + four chips](verdict-rename-and-four-chips.md) — Slice 5.0c plan: rename verdicts (Verified / Uncertain / Not Verified / Non-factual) and chip ALL four (today only 2 are chipped)
- [Progressive response + live activity feed](progressive-response-and-live-activity.md) — Slice 5.0c UX: token-by-token answer reveal (Claude-style) + step indicator narrating what Wolf is actually doing right now
- [Runbook authoring + actionable runbooks](runbook-authoring-and-actionable-runbooks.md) — Future Phase 5.5: web-based markdown editor for runbooks (organization + shared), tag-aware retrieval, runbook-prescribed propose-actions feeding Phase 6 approval gateway
- [Native HTTPS + wolf-cert CLI](native-https-and-wolf-cert.md) — Phase 5.4 (post-5.0c, pre-RBAC): self-signed CA + 100-year leaf certs, CLI lifecycle, unlocks secure-context APIs
- [Wolf colour palette](wolf-color-palette.md) — Slice 5.0c-c source of truth: Platinum / Dusk Blue / Steel Blue / Icy Blue; supersedes wolf-color-palette-outlook.png
- [wolf-pack (Phase 12)](wolf-pack.md) — bidirectional native daemon on every Wazuh host: ships rules/decoders/SCA/vuln/inventory INTO Wolf + executes propose-approved actions OUTBOUND. Renamed from wolf-knowledge-relay per ADR 0017 (ACCEPTED 2026-06-11); detailed design in a future ADR (~0024+, next free number — 0023 went to the dashboard edge-proxy)
- [Conversation tree persistence plan](conversation-tree-persistence-plan.md) — for the DB-storage phase: schema + atomic version-add + no path flattening + round-trip test; captured 2026-06-02 alongside Slice 5.0c-l v4.1
- [Repo public + hosted CI](repo-public-hosted-ci.md) — PUBLIC since 2026-06-12 by operator decision (billing); self-hosted CI fully reversed, don't re-propose; hardening list inside
- [Dependabot uv lock-only PRs](dependabot-uv-lock-only-prs.md) — uv "requirement update" PRs skip pyproject; run `uv lock --check` after merging, land floors by hand
- [Memory mirrored into repo](memory-mirrored-into-repo.md) — STANDING RULE (2026-06-13): every memory lives in BOTH ~/.claude memory dir AND in-repo memory/, kept byte-identical; repo copy git-tracked, never gitignored
- [Next dev cache vs build](next-dev-cache-vs-build.md) — never `rm -rf .next` OR `npm run build` while wolf-dashboard.service (next dev) runs (proxy 000s); AND after a prod build, next dev restarted on top serves a STALE client bundle — clear .next while STOPPED before restarting dev; AND a local `npm run build`+clear leaves stale VSCode `.next/types/*` "file not found" diagnostics (dev uses `.next/dev/types/`) → prefer tsc/eslint locally + CI for the build; reload the TS server if it happens
- [Same-network gate SHIPPED (6.5-h.2)](same-network-gate-deferred.md) — SHIPPED 2026-06-16 (ADR 0018 item 9; topology ADR 0023): stdlib TLS edge proxy fronts STOCK Next, owns the browser socket + stamps a trusted X-Wolf-Client-IP; wolf-server trusts it ONLY under mTLS + CIDR-checks vs its own NIC CIDRs (ifaddr). Gate **OFF by default** (on-prem-only control; default-ON would block remote MSSP clients); SAME_NETWORK_GATE_ENABLED=1 enables. Follow-ups: synced Superuser toggle (Phase 6.10) + per-org trusted networks (MSSP-correct)
- [Superuser config authority](superuser-config-authority.md) — STANDING RULE (2026-06-16): ALL Wolf management/configuration is Superuser-ONLY; org management → org admins; user settings → users; every config/management surface authorized by its owning role. Authorization half of web-first-configurability; generalizes wolf-bootstrap-superuser-flow
- [Config-settings system phase](config-settings-system-phase.md) — Phase 6.10 (planned 2026-06-16, implements ADR 0019; scope EXPANDED 2026-07-02): DB-source-of-truth runtime config synced file ⇄ Wolf CLI ⇄ Web GUI, Superuser-only, audited. Today config is env-only. Consumers: same-network gate toggle, "Model posture" (ADR 0024), "Grounding mode" (ADR 0026). EXPANDED (operator mandate): per-component config planes (server/database/dashboard each own central file) + Wolf config REACHES its tech stack (service-level Ollama knobs via privileged `wolf-tune` helper — no manual sudo rituals) + full three-way sync per plane; adjacent: per-org trusted networks
- [Input validation + exception handling](input-validation-exception-handling.md) — STANDING RULE (2026-06-15): every input field project-wide needs validation + guided, readable, field-relevant errors (server-authoritative + client-inline); dedicated retrofit slice for pre-rule fields
- [Scope + validation discipline](scope-and-validation-discipline.md) — STANDING RULE (2026-06-18, from Phase 6.6): interrogate EVERY parameter/field/opt-in/scope out-of-the-box (no scope unexplored), verify real-system behavior empirically before designing, precise guided message per failure mode, and ensure every behavior-affecting input is REFLECTED in its test/result surface (a test must change when the thing it tests changes); apply heavily in validation/messaging/scope-heavy phases
- [Notification + real-time phases](notification-and-realtime-phases.md) — operator-requested 2026-06-15: in-app Notification infra (future Phase 6.7) then SSE push (future Phase 6.8); notifications STRICTLY isolated from audit/logs
- [SMTP outbound email phase](smtp-outbound-email-phase.md) — Phase 6.9 (ADR 0022, future): provider-agnostic SMTP CLIENT (never an MTA) → free-tier ESP; deliverability via operator domain auth (SPF/DKIM/DMARC) + `wolf-mail doctor`; executes BEFORE 6.7 so notifications ship with an email channel
- [Phase 6.6 web-test plan](phase-6.6-web-test-plan.md) — Category-1 + Category-2 web-tests PASSED on the operator's real distributed cluster; 6.6-f (2026-06-18) reworked per-org scoping out of that test: credential-driven isolation (Wazuh RBAC + DLS), static org-id filter DROPPED → optional `agent.labels.group` injection, probe=index read, scope from RBAC policies; reserve Wazuh superusers for install-topology, dedicated scoped `wolf-<org>` users per org
- [Wazuh provisioning + collaboration phases](wazuh-provisioning-and-collaboration-phases.md) — operator-requested 2026-06-18: future Phase 6.11 (Wolf-assisted Wazuh RBAC provisioning + diagnostics, Superuser-only, Wolf's FIRST write-authority over Wazuh security config — ADR-worthy) + Phase 6.12 (per-org ↔ Superuser assistance/escalation, builds on the time-limited access grant, deps 6.7/6.8)
- [Wolf unrestricted / full power](wolf-unrestricted-full-power.md) — FOUNDATIONAL DIRECTIVE (2026-06-18): Wolf is NOT read-only; fully unrestricted + empowered; restriction comes from Wazuh's own RBAC (the credential's capabilities), not from Wolf limiting itself. Reshapes Phase 6 (propose+approval → capability-driven), the read-only client posture, Phase 13. Supersedes the original read-only framing
- [Web research as universal power](web-research-as-universal-power.md) — FOUNDATIONAL DIRECTIVE (2026-07-05): web_search/web_fetch/web_crawl = Wolf's go-to power for ALL operations, not a config-authoring accessory; research-to-act like Claude — unknown procedure → research docs-first w/ valid sources+references → learn in-session → act via capability-gated propose/approve/execute ("I want Wolf to be unstoppable"); provenance-backed learning (citations, untrusted envelope, grounding); authority model UNCHANGED (RBAC + approval gateway still gate every write); extends wolf-unrestricted-full-power to the knowledge dimension; recorded in ADR 0032 2026-07-05 addendum + roadmap 6-f
- [No hard step caps / unbounded persistence](no-hard-step-caps-unbounded-persistence.md) — OPERATOR DIRECTIVE (2026-07-06, 6-f.4 web-test), **SHIPPED same day (6-f.5)**: never hard-cap agent steps — the loop persists until answer; stops = no-progress guard / context-fit guard (0.8 × `effective_context_window()`: Ollama num_ctx, chain min) / optional `AGENT_STEP_BREAKER` (default off, 6.10 GUI); EVERY forced stop = `_synthesized_stop` best-effort synthesis (`budget_exhausted` retired); `max_safe_autonomous_steps` = soft checkpoint nudge cadence; web budget 12→32; SYSTEM_PROMPT #7 PERSIST UNTIL SATISFIED; the live 8-step wall was the failover-chain floor (min → qwen3:8b's 8)
- [Block disambiguation by any unique field](block-disambiguation-any-unique-field.md) — OPERATOR FEEDBACK (2026-07-06, 6-f.4 web-test), **SHIPPED same day (6-f.5)**: 3 same-name `<integration>` blocks → all upserts refused though hook_url/api_key distinguished them (model even tried block_key=hook_url — rejected, name-only matching → it hallucinated "identical hook_urls"); fix = `block_key` matches identity FIRST then ANY leaf value uniquely selecting one instance (one shared `_identified_matches` selector across tool/executor/proofs), B2 ops on ANY unblocked section, `content_carries_key` carry guard, >1-match refusals ENUMERATE the discriminating fields (`describe_instances`); only true duplicates stay hand-fix
- [Deployment-aware config application](deployment-aware-config-application.md) — OPERATOR DIRECTIVE (2026-07-06): config changes must be deployment-type-aware — all-in-one direct; distributed per component + per cluster node. GROUNDED GAP: `PUT /manager/configuration` writes the API-serving node (master) ONLY and Wazuh cluster sync does NOT replicate ossec.conf (rules/decoders/CDB ARE synced → rule_tuning fine) → workers keep old config on the 3-node cluster; fix = detect via /cluster/status + per-node `/cluster/{node_id}/configuration` (verify empirically first); indexer/dashboard config unreachable via Server API → wolf-pack (Phase 12), state the boundary honestly
- [Model switch to Nemotron after slices](model-switch-nemotron-after-slices.md) — OPERATOR INSTRUCTION (2026-07-06): AFTER 6-f.5 + 6-f.6 — if cohere/north-mini-code:free isn't right for agentic actions, switch to Nemotron 3 Ultra (free) then Nemotron 3 Super (free) on OpenRouter; verify exact IDs against the live catalog + tool-call probe + graded KNOWN_MODELS entry before flipping .env
- [Single-org ↔ MSSP parity](single-org-mssp-parity.md) — STANDING PRINCIPLE (2026-06-18): anything achievable in MSSP (multi-org) must ALSO work single-org (one broad-access credential, no DLS, filter OFF — already supported via the `unrestricted` scope path); don't bake MSSP-only assumptions into required fields/logic
- [Wazuh credential refinements](wazuh-credential-refinements.md) — from 6.6-f web-test (2026-06-18): Q1 credential-change validation bug FIXED; Q2 multiple comma-separated index patterns + per-index access checking BUILT (per-index ✓/✗ via _shards.total; clean enumeration of a scoped user's indices NOT possible — listing APIs 403); Q4 surface agent.labels.group in citations (still a tool-enrichment item)
- [Grounding execution modes (ADR 0026)](grounding-execution-modes.md) — 2026-06-21 (web-tested): configurable MODE — blocking / deferred (LIVE DEFAULT, answer-first chips-async) / incremental (concurrent batched chips, needs OLLAMA_NUM_PARALLEL>=2 else ~deferred). Env `GROUNDING_MODE` → Phase 6.10 GUI consumer #3. The `cited` evidence-scope was PULLED (name-keyed dedup starved the judge) → per-claim evidence selection deferred to [[grounding-enrichment-tools-future-phase]]. Frontend late-verdict patch via ref-guarded effect (no eslint disable)
- [wolf-server restart in harness](wolf-server-restart-in-harness.md) — REFERENCE: restarting wolf-server (:7860) from the Bash tool — DON'T `pkill -f "python -m wolf_server"` (self-matches the harness shell → exit 144, server may survive); kill by PID from `ss -ltnp | grep :7860`, wait via `tail --pid`, relaunch with `run_in_background` + absolute `--directory` + sourced repo-root `.env` (nohup/disown also fails); serves HTTPS → `curl -k …/auth/login`, 401 = up. Canonical `docs/restart.md` (uses pkill -f — caveat applies). Restart = part of the per-slice web-test handoff (stale code until restarted)
- [Phase 6-e.3 rule_tuning web-test (PASSED)](phase-6e3-rule-tuning-web-test.md) — PASSED 2026-06-30 on the live 3-node cluster (Tests A/B/C) AFTER fixing a verify bug it caught: write mechanism was sound (reload ~18s) but verify took GET /rules `items[0]` (the original) not the override entry → phantom "succeeded"; fix = authoritative `has_override` re-read before restart + honest fail + GUI evidence + dual-file `_resolve_rule` (commit 061d389, ADR 0029 §6). Reusable facts: admin `wazuh-wui` cred for writes (per-org lacks rules:update), approve RESTARTS cluster ~18s, GET /rules = on-disk parse returning original+override entries
- [Wolf UI/UX Claude-grade standard](wolf-uiux-claude-grade-standard.md) — STANDING FEEDBACK (2026-06-30): hold Wolf's web UI/UX to Claude's conversation UI/UX as the bar (dynamic/responsive/attractive/robust), applied strictly. Reusable fix patterns: scroll panels use native `min-h-0 flex-1 overflow-y-auto` NOT Radix ScrollArea (doesn't constrain in our flex chain); wide content stays inside the message via `min-w-0` on the markdown flex child + `overflow-x-auto` table wrapper; no truncate/"…" where full content is wanted; code-block-vs-inline-code discipline lives in the agent SYSTEM_PROMPT (MARKDOWN FORMATTING). Surfaced via rule_tuning web-test screenshots; honor [[next-dev-cache-vs-build]] when validating
- [Ollama num_ctx tool-truncation fix](ollama-num-ctx-tool-truncation.md) — REFERENCE (2026-07-01): Ollama's DEFAULT num_ctx=4096 silently truncates Wolf's ~7.2K-token system-prompt+tool-catalog (drops the HEAD) → model sees no tools → 0 tool calls + "no such tool" prose (the web-test regression). Fix = `Settings.ollama_num_ctx` (default 16384, `OLLAMA_NUM_CTX`) passed to chat AND judge builds (shared knob = one loaded context, no reload); regression test pins it onto `OllamaAdapter._num_ctx`. Re-check whenever the tool catalog grows or a new local default lands. Ignored by hosted providers. TRADE-OFF on VRAM-tight GPUs: 16384 enlarges KV-cache → CPU spill → ~25% slower gen (6GB card: 16384=14.4 vs 4096=19.3 tok/s); recover via KV-cache quantization (`OLLAMA_FLASH_ATTENTION=1`+`OLLAMA_KV_CACHE_TYPE=q8_0`, Ollama SERVICE). FULL tuning surface (num_ctx · KV-quant · posture · NUM_PARALLEL · grounding mode + scenario recipes + apply/revert) → `docs/reference/model-performance-tuning.md`
- [Grounding concurrency model](grounding-concurrency-model.md) — STANDING PRINCIPLE (2026-07-01, ADR 0026 addendum): grounding is per-request PARALLEL/concurrent for every org/user/thread/message (the "serve everyone like Claude" bar; foundational to MSSP). ALREADY the architecture (each chat = own asyncio task + own judge/validator/DB session; NO lock/semaphore/queue on the grounding path — verified). NEVER serialize/queue grounding (a FIFO queue was proposed 2026-07-01 + REJECTED before any code — don't re-propose). App layer unbounded; ONLY `OLLAMA_NUM_PARALLEL`+VRAM (infra) governs simultaneous execution — enterprise dials it up, no code change
- [OpenRouter quota visibility (reactive)](openrouter-quota-visibility.md) — REFERENCE (2026-07-01, ADR 0031 addendum): hardcode the CONTRACT not the NUMBERS (free cap flips 50→1000/day on ≥$10 credits). Reactive half SHIPPED — `ProviderQuota` parsed from 429/402 `X-RateLimit-*` headers (Reset=epoch MS) + body → quota-aware `_model_failure_message`; 402 = `ModelProviderPaymentRequiredError`; failover log records quota. Proactive `/key`+`/credits` dashboard indicator + successful-failover in-chat chip DEFERRED to per-org OpenRouter phase (dormant under Ollama default → untestable now)
- [Model-failure resilience + OpenRouter free reality + failover chain](model-failure-resilience-and-openrouter-free-reality.md) — REFERENCE (2026-07-01, ADR 0031): (1) SSE chat MUST degrade gracefully on ANY model failure (never hang) — `_fail_gracefully`; WolfError re-raises ONLY in blocking mode; NEVER a bare raise on the streaming path. (2) OpenRouter FREE = VERIFIED hard cap **50 req/DAY per-ACCOUNT shared across ALL `:free` models** (`/key` is_free_tier=true, X-RateLimit-Limit:50) → no free model escapes it; reliable OpenRouter = PAID, private+uncapped = local Ollama. (3) `FailoverProvider` (`models/failover.py`) — chain [primary→fallback], fails over on ANY non-cancel error (CancelledError propagates→Stop safe), clean pre-stream failover, per-instance breaker, conservative capability floor; wired via `FALLBACK_MODEL_*` (chat+judge). DEFAULT = local Ollama qwen3:8b (chat+judge, no chain); per-org OpenRouter→Ollama failover = later phase. Live-verified 429→Ollama tool call. Alongside: empty-message-400 fix (skip empty history turns + `_sendable`), repeated-tool-call guard, `grounding_unavailable` chip
- [Web research phase](web-research-phase.md) — **ADR 0032 LANDED** (2026-07-03, operator-directed from the 6-e.4 web-test): Wolf web-research + config-authoring generalization. Provider-agnostic `web_search`/`web_fetch`/**bounded `web_crawl`** (agent-loop-chained, model-decided like Claude; crawl = query-driven, same-domain, depth/page-capped, robots-respecting, NEVER an unbounded spider), **SearXNG self-hosted FREE DEFAULT** behind a pluggable `SearchProvider` adapter (Brave/Tavily optional per-org), docs-first→community fallback, citations→existing evidence panel. **`wolf-search` = its OWN native-venv Debian package MIRRORING wolf-database** + **wolf-server SIDECAR** (loopback in single-server AND default-distributed = zero net exposure; mTLS-required only for an optional dedicated tier; `SEARXNG_URL` flips like `DATABASE_URL`/`OLLAMA_BASE_URL`, ADR 0016). **14-class security taxonomy** (SearXNG web access is FREE/uncapped — the budgets are self-protection + tenant-fairness, NOT a paywall; denial-of-wallet = PAID backends only). Config-authoring = research→confirm-diff→dry-run-validate→propose + **block-identity** for repeated `<integration>` (the virustotal fix). **6-f.1 SHIPPED 2026-07-03** (`research/` scaffolding: protocol+SearXNG adapter+resolver, fails closed when disabled, 798 tests/0 skip, research/ in strict set); 6-f.2 HOST HALF DONE 2026-07-03 (wolf-search LIVE 127.0.0.1:1307, official install pinned 747cec4c, uWSGI + dedicated wolf-search.service, `wolf-search health` healthy (renamed from doctor by operator request), adapter parsed live JSON 5/5); 6-f.2 CLOSED 2026-07-03 (packaging half: debian/wolf-search.* postinst = the pinned recipe, Recommends in wolf meta, 5 .debs, smoke-deb-install starts service + health end-to-end); **6-f.3 SHIPPED 2026-07-05 + operator web-test PASSED 2026-07-05** (the 3 tools LIVE: SSRF guard w/ pinned-IP+SNI + per-hop redirect re-validation, decompressed-cap fetcher, docs-first re-rank + EMPTY operator blocklist, bounded crawler (robots/sitemap/same-domain/politeness/caps), untrusted-content envelope, per-request budget, NEW ToolDegradedError dispatcher branch, Citation url/title/source + evidence-panel links w/ official badges, WEB_RESEARCH_SUFFIX prompt gated w/ registration on WEB_SEARCH_ENABLED alone (reachability = call-time degradation), 7 A7 knobs, 866 tests/0 skip, live-validated 1 search + 3 chained fetches w/ documentation.wazuh.com #1; .env flag stays ON — feature operator-accepted; web-test observation → grounding-enrichment became committed Phase 6.13); **6-f.4 SHIPPED 2026-07-05** (config-authoring generalization: B3 blocklist {auth,cluster,indexer,rule_test,ruleset} + add-if-absent · B2 block-identity `upsert_block`/`remove_block`+`block_key` w/ IDENTITY_KEYS {integration→name, localfile→location, command→name}, identity-carry guard, identity-scoped persist proofs, block_key-aware restore · B1 confirm-diff TWO-PHASE IN THE TOOL (unconfirmed call = full author-time work + `needs_confirmation` preview w/ current_content, queues NOTHING; author-time manager validation impossible without a write → dry-run = shared `build_candidate` transformation, manager validation at execute w/ auto-rollback) + RESEARCH-TO-ACT prompt posture per [[web-research-as-universal-power]]; 896 tests/0 skip; live-validated: qwen3:8b chained runbook→propose upsert_block/virustotal unprompted, capability refusal honest; **operator web-test = virustotal end-to-end PENDING** — needs the `manager:update_config` credential + qwen3:8b (live cohere/north-mini-code:free emitted tool call as prose, ADR 0031 reality)); **6-f.5 SHIPPED 2026-07-06** (web-test feedback fixes: unbounded persistence + any-unique-field disambiguation → see the two dedicated memories; 908 tests/0 skip; NEXT = 6-f.6 deployment-aware config application); unit tests STUB the HTTP boundary (hermetic CI), live SearXNG only in web-tests. Sequenced AFTER the 6-e.4 config fixes = DONE (commit ecc3562)
