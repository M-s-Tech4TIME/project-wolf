Wazuh Credentials for the Cluster (TEMPLATE — replace every CHANGE_ME):
========================================================================

# Deployment URLs — one line per node. All-in-one deployments have one of
# each; distributed clusters list every node (Wolf's config_change applies
# per node on clusters).
Wazuh Indexer API URL - 1: https://CHANGE_ME:9200
Wazuh Indexer API URL - 2: https://CHANGE_ME:9200
Wazuh Indexer API URL - 3: https://CHANGE_ME:9200
Wazuh Server API URL - 1 (Master Node): https://CHANGE_ME:55000
Wazuh Server API URL - 2 (Worker Node): https://CHANGE_ME:55000
Wazuh Server API URL - 3 (Worker Node): https://CHANGE_ME:55000
Wazuh Dashboard - 1: https://CHANGE_ME
Wazuh Dashboard - 2: https://CHANGE_ME

# Admin user for the Wazuh dashboard + indexer. Printed by the Wazuh
# installation assistant (wazuh-install.sh prints all internal-user
# passwords at install time; also recoverable from
# wazuh-install-files.tar / wazuh-passwords-tool).
  indexer_username: 'admin'
  indexer_password: 'CHANGE_ME'

# Wazuh Server API admin user (manager-global RBAC: rules:update,
# manager:update_config, cluster:update_config — required for Wolf's
# rule_tuning and config_change action classes).
  api_username: 'wazuh-wui'
  api_password: 'CHANGE_ME'

Per-Organization Scoped Users (one pair per Wolf organization):
===============================================================
# Create a dedicated 'wolf-<org-slug>' user in BOTH the indexer (with a
# DLS-scoped role limiting it to the org's agents/indices) and the Server
# API (with an RBAC role scoped to the org's agent group). Wolf's
# capabilities per org are exactly this credential's capabilities —
# that is the security model (see docs/decisions/0025 + memory
# phase-6.6-web-test-plan). Reserve the Wazuh superusers above for
# install topology, not for org wiring.

<org-slug-1>
------------
indexer_username: wolf-CHANGE_ME
indexer_password: CHANGE_ME
api_username: wolf-CHANGE_ME
api_password: CHANGE_ME

<org-slug-2>
------------
indexer_username: wolf-CHANGE_ME
indexer_password: CHANGE_ME
api_username: wolf-CHANGE_ME
api_password: CHANGE_ME
