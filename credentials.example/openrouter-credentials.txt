OpenRouter Credentials (TEMPLATE — replace CHANGE_ME):
======================================================

# OPTIONAL — only needed when running a hosted model via OpenRouter
# (DEFAULT_MODEL_PROVIDER=openrouter). Local Ollama needs no credentials.
#
# Get an API key at https://openrouter.ai/keys. The key is stored in
# Wolf's encrypted secrets backend under the reference
# `model.openrouter.api_key` (referenced from .env as
# DEFAULT_MODEL_API_KEY_REF) — never in the DB or in .env directly.
#
# Free-tier reality (ADR 0031): a hard cap of 50 requests/DAY per ACCOUNT
# shared across ALL :free models ($10+ in credits raises it to 1000/day).
# Reliable OpenRouter use = paid credits; private + uncapped = local Ollama.

username: CHANGE_ME
password: CHANGE_ME
API-Key: CHANGE_ME
