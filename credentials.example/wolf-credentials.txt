Wolf application logins (TEMPLATE — replace every CHANGE_ME):
=============================================================

# Organization admin users — created by bootstrap_organization
# (ONBOARDING.md §3.9). One line per org:
#   <Org Name>  <admin email>  <password>  <role>
<Org Name 1>	CHANGE_ME@example.com	CHANGE_ME	Tenant Admin
<Org Name 2>	CHANGE_ME@example.com	CHANGE_ME	Tenant Admin

# Additional per-org users (created by an org admin in the web UI):
#   <email> <password>
CHANGE_ME@example.com CHANGE_ME

# The bootstrap Superuser "Wolf" — the ONLY role that can create
# organizations/users/roles and configure Wazuh component mapping.
# Its password is AUTOGENERATED at install/bootstrap and printed once —
# record it here (memory: wolf-bootstrap-superuser-flow).
      username : Wolf
      password : CHANGE_ME
