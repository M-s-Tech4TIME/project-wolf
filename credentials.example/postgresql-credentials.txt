PostgreSQL credentials for Wolf (TEMPLATE — replace CHANGE_ME):
================================================================

# The dedicated database + owner role for wolf-server, with pgvector
# enabled. On the recommended wolf-database path the password is
# GENERATED and printed once by `make wolf-database-init` — record it
# here. On the system-Postgres path you choose it when creating the role
# (ONBOARDING.md §3.4 Path B).
#
# Goes into .env as:
#   DATABASE_URL=postgresql+asyncpg://wolf:<password>@localhost:5432/wolf

Database Name: wolf
Owner: wolf
Password: CHANGE_ME
