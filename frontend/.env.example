# Wolf frontend — environment template.
# Copy to .env.local (gitignored) and adjust.

# URL of the Wolf orchestrator HTTP API.  The frontend talks to this with
# `credentials: "include"` so the orchestrator's CORS config must allow
# this origin (localhost:3000 is allowed in development by default).
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:8000
